# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
aiofiles>=23.2.0
numpy>=1.24.0
Pillow>=10.0.0
psutil>=5.9.0

# Furniture Search (RAG/NLP)
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
pandas>=2.0.0

# Authentication & Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
email-validator>=2.0.0

# ML/3D Processing (requires Python 3.11 or 3.12 for full support)
torch>=2.1.0
torchvision>=0.16.0
opencv-python>=4.9.0
trimesh>=4.0.0
# open3d>=0.18.0  # Uncomment if using Python 3.11/3.12 (not supported on 3.13)

# Depth Anything V3 (from GitHub)
# Note: If you get "Cannot uninstall blinker" error, use: pip install --ignore-installed blinker git+https://github.com/ByteDance-Seed/Depth-Anything-3.git
git+https://github.com/ByteDance-Seed/Depth-Anything-3.git

# YOLOv8 for furniture detection
ultralytics>=8.0.0

# Note: gsplat must be installed separately for Gaussian Splatting:
# pip install --no-build-isolation git+https://github.com/nerfstudio-project/gsplat.git@v1.0.0
